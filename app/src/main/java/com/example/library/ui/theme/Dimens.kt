package com.example.library.ui.theme

import androidx.compose.ui.unit.dp

/**
 * Dimension tokens — corner radii, icon sizes, component heights.
 */
object Dimens {

    // --- Corner Radius ---
    /** 8dp — small chips, badges */
    val CornerSmall = 8.dp
    /** 12dp — buttons, text fields */
    val CornerMedium = 12.dp
    /** 16dp — list items, cards (small) */
    val CornerCard = 16.dp
    /** 22dp — cover art inner box */
    val CornerCoverInner = 22.dp
    /** 28dp — book cards (outer) */
    val CornerCardLarge = 28.dp
    /** 50% — pill / circle shapes */

    // --- Icon Sizes ---
    /** 18dp */
    val IconSmall = 18.dp
    /** 24dp — default icon */
    val IconMedium = 24.dp
    /** 28dp — top bar action icons */
    val IconLarge = 28.dp
    /** 56dp — empty state icons (medium) */
    val IconXLarge = 56.dp
    /** 64dp — profile avatar, empty state */
    val IconHuge = 64.dp
    /** 80dp — large empty state */
    val IconMassive = 80.dp

    // --- Component Sizes ---
    /** 56dp — standard button height / search bar */
    val ButtonHeight = 56.dp
    /** 80dp — bottom navigation bar height */
    val BottomNavHeight = 80.dp
    /** 380dp — bottom navigation bar width */
    val BottomNavWidth = 380.dp
    /** 50dp — notification button size */
    val NotificationButtonSize = 50.dp
    /** 26dp — notification button icon size */
    val NotificationIconSize = 26.dp
    /** 8dp — notification badge dot */
    val BadgeDotSize = 8.dp
    /** 100dp — profile avatar size */
    val ProfileAvatarSize = 100.dp
    /** 120dp — empty library icon container */
    val EmptyIconContainer = 120.dp
}
